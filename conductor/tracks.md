# 任务追踪 (Track Registry)

## 进行中 (Active Tracks)
- [ ] brand-identity-setup: 项目基础品牌信息配置（`index.html` Title/Description 改写，定制专属 SVG Favicon 替换 Vite 默认图标）。

## 待办 (Backlog)
- [x] mobile-layout-adaptation: 移动端全面适配（涵盖 `App.vue/MainLayout.vue` 响应式改造、移动端 `Sidebar` 抽屉菜单滑入交互及 UI Store 状态管理）。
- [x] year-selector-fixes: 修复全局年份选择器在不相关的页面显示的问题，以及下拉菜单被底层元素遮挡的 z-index 渲染层级问题。
