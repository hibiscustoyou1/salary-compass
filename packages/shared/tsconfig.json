{
  "extends": "@repo/tsconfig/node.json",
  "compilerOptions": {
    "outDir": "./dist",
    "declaration": true,
    // 允许从 CommonJS 模块导入 (继承自 base/node，但此处可显式保留以防万一)
    "allowSyntheticDefaultImports": true
  },
  "include": ["src"],
  "exclude": ["node_modules", "dist"]
}
